const API=import.meta.env.VITE_API_URL ?? 'http://localhost:3000/api';
export function getToken(){return localStorage.getItem('hrmis_token')}
export async function api<T>(path:string,init:RequestInit={}):Promise<T>{const r=await fetch(`${API}${path}`,{...init,headers:{'Content-Type':'application/json',...(getToken()?{Authorization:`Bearer ${getToken()}`}:{ }),...(init.headers||{})}});const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.message||'Request failed');return body as T}

export async function apiUpload<T>(path:string,file:File):Promise<T>{const form=new FormData();form.append('file',file);const r=await fetch(`${API}${path}`,{method:'POST',headers:{...(getToken()?{Authorization:`Bearer ${getToken()}`}:{})},body:form});const body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.message||'Upload failed');return body as T}
export function authenticatedDownload(path:string,filename?:string){fetch(`${API}${path}`,{headers:{...(getToken()?{Authorization:`Bearer ${getToken()}`}:{})}}).then(async r=>{if(!r.ok){const b=await r.json().catch(()=>({}));throw new Error(b.message||'Download failed')}return r.blob()}).then(blob=>{const u=URL.createObjectURL(blob);const a=document.createElement('a');a.href=u;a.download=filename||'attachment';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(u)}).catch(e=>alert(e.message||'Unable to download attachment'))}
