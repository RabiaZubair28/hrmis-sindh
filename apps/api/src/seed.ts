import { NestFactory } from '@nestjs/core'; import { AppModule } from './app.module'; import { UsersService } from './users/users.service'; import { Role } from './common/roles'; import * as bcrypt from 'bcryptjs';
async function run(){const app=await NestFactory.createApplicationContext(AppModule);const users=app.get(UsersService);const email=process.env.SEED_ADMIN_EMAIL;const password=process.env.SEED_ADMIN_PASSWORD;if(!email||!password||password.length<12)throw new Error('Set SEED_ADMIN_EMAIL and SEED_ADMIN_PASSWORD (12+ chars)');if(await users.findByEmail(email)){console.log('Admin already exists');await app.close();return;}await users.create({email,personnelNo:process.env.SEED_ADMIN_PERSONNEL_NO||'ADMIN-001',passwordHash:await bcrypt.hash(password,12),role:Role.SUPER_ADMIN,isActive:true,permissions:['*']});console.log('Super admin created');await app.close();}run();
